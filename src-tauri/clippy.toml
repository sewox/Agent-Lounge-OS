disallowed-methods = [
  { path = "std::process::Command::new", reason = "use crate::kernel::guarded_command::GuardedCommand — PolicyGate must wrap all process spawns (PR-1 G1)" },
  { path = "tokio::process::Command::new", reason = "build via GuardedCommand::into_std_command then tokio::process::Command::from (PR-1 G1)" },
]
