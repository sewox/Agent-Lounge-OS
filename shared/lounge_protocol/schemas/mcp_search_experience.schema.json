{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-lounge-os.local/schemas/mcp_search_experience.schema.json",
  "title": "McpSearchExperienceArgs",
  "description": "lounge_search_experience tool girdileri. Serbest biçim reddedilir.",
  "type": "object",
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": { "type": "string", "minLength": 1 },
    "project": { "type": "string", "minLength": 1 },
    "project_id": { "type": "string", "minLength": 1 },
    "active_file": {
      "type": "string",
      "minLength": 1,
      "description": "Açık dosya yolu — project_index ile otomatik project_id eşlemesi"
    },
    "workspace_root": {
      "type": "string",
      "minLength": 1,
      "description": "Çalışma alanı kökü — project_index ile otomatik project_id eşlemesi"
    },
    "limit": { "type": "integer", "minimum": 1, "maximum": 50 }
  }
}
